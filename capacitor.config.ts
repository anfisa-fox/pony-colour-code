import type { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'dev.anfisafox.ponycolourcode.beta',
  appName: 'Pony Colour Code',
  webDir: 'dist'
};

export default config;
